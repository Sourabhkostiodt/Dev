import { CapitaltextPipe } from './capitaltext.pipe';

describe('CapitaltextPipe', () => {
  it('create an instance', () => {
    const pipe = new CapitaltextPipe();
    expect(pipe).toBeTruthy();
  });
});
